import { Directory } from "../../components";
import { HomePageContainer } from "./home-page.styles";

export const HomePage = () => {
  return (
    <HomePageContainer>
      <Directory />
    </HomePageContainer>
  );
};
